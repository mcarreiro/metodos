#include <vector>
#include <cmath>
class Hits
{
public:
    Hits( vector<vector<int> >&);
    void hubsYautoridades();
    vector<double> x;
    vector<double> y;
private:
    DOK matriz;
    vector<double> normalizarVector(vector<double>);
};
